kind = "api"
previewPath = "/"
title = "Vertex AI Proxy"
version = "1.0.0"

[[services]]
localPort = 8000
name = "Vertex AI Proxy"
paths = ["/"]

[services.development]
run = "PORT=8000 python3 main.py"

[services.production]

[services.production.run]
args = ["bash", "-c", "python3 -m pip install -r artifacts/vertex-proxy/requirements.txt -q && cd artifacts/vertex-proxy && python3 main.py"]

[services.production.run.env]
PORT = "8000"

[services.production.health.startup]
path = "/health"
